# Mujun Orbit · 快捷轮盘

一个只做四向快捷操作的 Windows 后台小工具。

## 使用

1. 按住鼠标右键。
2. 在右键未松开的情况下点一下左键。
3. 保持右键按下约 180 毫秒，等待四向菜单出现。
4. 将鼠标移动到目标扇区。
5. 松开右键执行；移动回中央或按 `Esc` 取消。

普通右击仍会打开原来的上下文菜单。只按右键并拖动超过少量距离时，操作会交还给当前软件。全屏程序中默认禁用轮盘。

## 默认动作

- 上：恢复或呼出最近使用的 Codex 窗口；Codex 未运行时启动应用。
- 右：Windows 区域截图。
- 下：显示或恢复桌面。
- 左：Windows 剪贴板历史。

双击通知区域中的“四向轮盘”图标，可以修改四个方向、180 毫秒等待时间、菜单尺寸、全屏禁用和开机启动。托盘菜单也提供暂停和退出。

## 运行与发布

- 当前交付程序：`artifacts\MouseRing-win-x64\MouseRing.exe`
- 源码运行：`./run-mousering.ps1`
- 构建与测试：`./build-mousering.ps1`
- 重新发布：`./publish-mousering.ps1`

当前交付采用框架依赖发布，以减少程序体积；这台电脑已经安装所需的 .NET 10 Desktop Runtime。
