# Mujun Pal · 桌面伙伴

Pal 是 MU Desk 内的 Windows x64 桌面伙伴，工程与工作进程名称保留 LightPet。不依赖 VPet，也不包含养成、商店、Steam、语音、插件系统或后台大模型调用；改名不改变角色资源和已有配置。

## 当前完成度

- 透明、置顶、无边框、任务栏隐藏的 WPF 桌宠窗口。
- 249 张 1000×1000 透明母版、249 张 600×600 发布帧，32 组逻辑动作（左右行走使用两套独立朝向素材，另有 4 个贴边保持状态）。
- 固定脚底锚点，不按单帧外接框重新居中。
- 逐帧不等时长，支持 `Start / Loop / End / Single`。
- 待机循环已从 1.6 秒放缓到 3.0 秒，不再用角色整体平移冒充呼吸。
- 左右行走、摸头、拖拽提起/落地、触碰身体、长按反馈、启动/离场、睡眠、思考、说话、情绪、专注与招手。
- 行走使用两轮完整步态和起停缓动，窗口位移与动画真实总时长同步。
- 自主移动可沿当前显示器工作区的下、左、上、右四边巡游：底边正常步行，左右边缘使用独立攀爬动作，顶边使用独立向左/向右悬挂移动动作；拐角按顺时针或逆时针连续换边。
- 停在侧边或顶边时保持对应贴边姿势，不会恢复成悬空站立；拖到屏幕中间后，下次自主移动会先自然回落到底边。
- 播放器使用绝对截止时间轴，操作系统调度延迟不会逐帧累积成动画与窗口位移脱节。
- 按当前显示器工作区限制位置；到屏幕边缘自动转向，不进入任务栏或屏幕外。
- 屏幕安全范围按角色可见轮廓而不是整张透明画布计算；四边巡游目标缝隙约 2 px，透明窗口可按素材留白向屏幕外延伸，因此不会再离边缘约 40 px。
- 从身体中段拖拽时使用原有的横向提起 `raise` 姿势；释放后自动吸回可见工作区并播放落地动作。
- 拖动以屏幕坐标锁定原始抓取点，不受移动窗口的局部坐标反馈影响，不再左右往返抽动。
- 14–32 秒一次的低频自主活动，以细微待机为主。
- 连续 30–60 秒没有用户操作后，低优先级待机分支可能播放伸懒腰、整理头发、打哈欠或坐下休息；点击、拖动会立即中断，不带台词，也不会驱动窗口位移。
- 鼠标靠近时低频观察；头部来回移动或长按触发持续摸头，脸颊点击触发戳脸，连续快速点击四次会躲开并表现不满。
- 头部抚摸与身体拖动分流，避免“想摸头却把宠物拖走”；右键角色可直接打开提醒、便签、边缘巡游、互动和尺寸菜单。
- 普通气泡台词完全由本地词库驱动，不调用网络或 LLM。
- 提醒与便签已经拆成独立的 `ReminderNotes` 工具；双击桌宠或从通知区选择入口时，LightPet 只负责启动工具并请求打开对应页面。
- 独立工具自己负责提醒调度、系统托盘通知、窗口和本地数据；退出或升级桌宠不会中断它的职责边界。
- 提醒支持明确日期时间、10/30/60 分钟与明早 9 点预设、完成、稍后 10 分钟和删除；便签支持正文、标签、最近列表、归档和删除。
- 气泡是独立透明浮窗：普通台词使用无标题的 198×64 漫画对白规格，提醒与保存结果使用带小型身份标记的 232×88 通知规格；短三角尾巴跟随角色位置，靠近屏幕边缘时自动换到左侧、右侧或下方，并始终鼠标穿透。
- 原生 Windows 托盘：提醒与便签入口、“沿屏幕边缘移动”即时触发、33 个动作预览条目（左右行走合计 1 组，对应 32 组逻辑动作）、130/160/200/260/320/400 六档大小、点击穿透、自主活动和退出。
- EXE、快捷方式与通知区共用多尺寸角色头像图标（16–256 px），不再显示通用 Windows 图标。
- 单实例、位置/大小/开关持久化、数据型角色包和严格路径/PNG/帧时长校验。

## 32 组逻辑动作

`idle`、`idle-random`、`walk-left / walk-right`、`raise`、`fall-land`、`touch-head`、`touch-body`、`pinch`、`startup`、`shutdown`、`sleep`、`think`、`say`、`happy`、`sad`、`surprised`、`focus`、`wave`、`reminder-alert`、`note-write`、`edge-climb-left`、`edge-climb-right`、`edge-top-left`、`edge-top-right`、`observe-cursor`、`head-rub`、`cheek-poke`、`annoyed-dodge`、`stretch`、`hair-fix`、`yawn`、`sit-rest`。角色包另包含 4 个单帧贴边保持状态，不计为独立逻辑动作。

## 运行

只想试用时，解压 `artifacts\LightPet-win-x64.zip` 后直接运行其中的 `LightPet.exe`。此为框架依赖版，需要 Windows x64 和 .NET 10 Desktop Runtime。

从源码构建：

```powershell
.\build-lightpet.ps1
.\run-lightpet.ps1
```

可执行文件：

```text
src\LightPet.App\bin\Release\net10.0-windows\LightPet.exe
```

右键通知区图标可打开提醒与便签、全部动作预览和设置。角色开启点击穿透后，仍可从通知区关闭点击穿透。

桌宠设置保存在 `%LocalAppData%\LightPet\data`。提醒与便签保存在独立的 `%LocalAppData%\ReminderNotes\data`；独立工具首次运行时会复制旧 LightPet 目录中的已有提醒和便签。

生成可分发压缩包：

```powershell
.\package-lightpet.ps1
```

打包流程保留 `pets/` 下的 1000 px RGBA 美术母版，只在发布目录中确定性降采样为 600 px，并转换为带透明度的 256 色 PNG。量化步骤有 PSNR、逐帧平均误差和最大像素误差硬门槛，之后仍运行完整动画 QA。应用只使用 WPF 与 Win32 P/Invoke，因此目标框架采用通用 `net10.0-windows`，不携带未使用的 Windows SDK/WinRT 投影程序集；最终压缩包约 3.2 MB。

## 动画 QA

```powershell
py -3.12 tools\lightpet_animation_qa.py pets\violet-alex-benchmark --output work\qa-full
```

输出包括逐帧 JSON、问题摘要、逐动作接触表、完整动作 GIF、实际窗口位移 GIF、拖拽释放链预览、调色板异常热图、Alpha 连通区域异常图、最小显示尺寸边距表、相邻帧运动节奏表和全动作总览。当前角色包结果为 249 帧、0 错误、0 警告；检查项包含源图与 130 px 实际显示边距、空白帧、画布尺寸、主体连通性、游离碎片、孤立 Alpha 像素、相邻帧视觉位移、按帧时长归一化的运动速率、孤立速度尖峰、脚底/身体漂移、待机速度、阶段切换、待机进出、贴边保持、提起横向姿态、释放落地纵向姿态、交互链尺寸跳变和跨生成批次的前景色域覆盖率。

最终发布包还在真实 WPF 透明窗口中按 130、160、170、200、260、320、400 px 七档复核；声明最小尺寸、触头宽轮廓及 400 px 左右行走均无裁切，深浅背景交界处无矩形底或明显色边。

运行时回归：

```powershell
.\tools\test_lightpet_edges.ps1
.\tools\test_lightpet_frame_timing.ps1
.\tools\test_lightpet_action_tour.ps1
.\tools\test_lightpet_soak.ps1 -Minutes 5
.\tools\test_lightpet_notice_layout.ps1
```

## AI 边界

当前版本不接入大模型。后续即使增加 AI 对话，也只允许在用户显式打开对话并点击发送时调用 API；待机、点击、拖拽、随机动作和普通台词始终保持本地规则驱动。
