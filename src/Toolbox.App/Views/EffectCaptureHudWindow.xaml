<Window x:Class="PersonalToolbox.Views.EffectCaptureHudWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ShowInTaskbar="False" ShowActivated="False" Topmost="True" ResizeMode="NoResize">
    <Border Padding="10,7" CornerRadius="8" Background="#F21A1C24"
            BorderBrush="#55FFFFFF" BorderThickness="1">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Ellipse Width="8" Height="8" Margin="0,0,7,0" Fill="#FF5D67" />
                <TextBlock x:Name="StatusText" Foreground="White" FontFamily="Microsoft YaHei UI" Text="准备录制" />
            </StackPanel>
            <StackPanel x:Name="ActionPanel" Grid.Column="1" Orientation="Horizontal" Margin="12,0,0,0" Visibility="Collapsed">
                <Button Padding="11,4" Background="#8055D9" Foreground="White" BorderBrush="#A77CF0"
                        Content="停止" Click="Stop_OnClick" />
                <Button Margin="6,0,0,0" Padding="11,4" Background="#272A34" Foreground="White" BorderBrush="#555A68"
                        Content="取消" Click="Cancel_OnClick" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
